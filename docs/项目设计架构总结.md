# 商场可视化智能车位/商铺导航系统 —— 项目设计架构总结

> 本文件是当前会话（截至 2026-08-17）项目状态与设计的完整总结，供 DeepSeek Harness 等新执行环境接手使用。
> 仓库：`C:\Users\54316\Desktop\projects\positioning-backend`；git HEAD `0e16a4a`。

## 1. 项目说明

**定位**：商场可视化智能车位/商铺导航系统。

**核心价值**：用户进入商场地下车库后，输入车位号或目标商铺名称，系统通过**蓝牙室内定位 + 路径可视化**，指引用户"往左走20米，右转进入B区"。

**技术壁垒**：空间数据的组织与可视化呈现（而非高并发）。

**核心场景**：
1. 蓝牙信标扫描上报 RSSI，确定用户所在楼层与坐标；
2. 输入车位号/商铺名模糊搜索；
3. 路径规划并返回带几何的路径与分段指令；
4. 停车记录、收藏、导航历史、运营方空间数据维护。

## 2. 数据库设计

### 2.1 总体

- PostgreSQL 16 + PostGIS 3.x；单库 `postgis_36_sample`，两个架构：`auth`（用户中心）、`business`（业务）。
- 共 24 张表：auth 6 张 + business 18 张，**所有字段均有中文注释**（`COMMENT ON`）。
- 主键：应用层雪花 ID（BIGINT）；**不建物理外键**，跨表/跨架构关联只建索引，由应用保证一致性。
- 逻辑删除：`deleted`（0/1），部分唯一索引 `WHERE deleted = 0`。

### 2.2 坐标与空间建模

- 室内几何统一"楼层本地平面坐标系"，单位米，SRID=0；距离用 `ST_Distance` 直接得米，无投影畸变。
- 分层：`mall → mall_floor → mall_zone → 空间要素`。
- 空间要素三几何列：`geom`（轮廓，落区判断）、`center_point`（中心，KNN 就近检索）、`entrance_point`（入口，导航终点）。
- 导航图：`nav_node`（节点+`is_accessible` 封路）、`nav_edge`（边长冗余 `distance_m`、成本权重 `weight`、`bidirectional`）、`floor_connect`（跨楼层绑定+折算成本 `cost_m`）。
- 文本搜索：`pg_trgm` GIN 索引（shop_name/keywords/space_no）。

### 2.3 表清单

**auth 架构**：sys_user、sys_role、sys_permission、sys_user_role、sys_role_permission、sys_login_log。

**business 架构**：mall、mall_floor、mall_zone、parking_space、shop_category、shop、poi、beacon、nav_node、nav_edge、floor_connect、mall_user、user_vehicle、parking_record、user_destination、nav_history、positioning_session、op_log。

### 2.4 SQL 脚本

| 脚本 | 职责 | 备注 |
| --- | --- | --- |
| sql/00_init.sql | 安装 postgis、pg_trgm 扩展 | **以用户最新版本为准，不得改动** |
| sql/01_auth_db.sql | 建 auth schema 与 6 张表 | 含内置角色种子数据 |
| sql/02_business_db.sql | 建 business schema 与 18 张表 | |

## 3. 架构设计

### 3.1 技术栈与版本（已验证可解析、可编译）

| 组件 | 版本 |
| --- | --- |
| JDK | 21 |
| Spring Boot | 3.5.16 |
| Spring Cloud | 2025.0.3 |
| Spring Cloud Alibaba | 2025.0.0.0（Nacos 客户端 3.0.3） |
| Sa-Token | 1.45.0（sa-token-jwt，JWT 无状态） |
| MyBatis-Plus | 3.5.12（+ mybatis-plus-jsqlparser 分页模块） |
| PostgreSQL 驱动 | Boot 管理（42.7.x） |
| 缓存/限流 | Caffeine（网关限流） |
| 熔断 | Resilience4j（spring-cloud-starter-circuitbreaker-reactor-resilience4j） |

### 3.2 模块结构（Maven 聚合，128 个 Java 文件）

```text
positioning-backend
├── pom.xml                              # 父工程：版本管理 + dependencyManagement
├── positioning-common                   # 公共模块（不依赖 Web 容器）
│   └── com.positioning.common
│       ├── api/Result.java              # 统一响应 {"code","message","data"}
│       ├── api/PageResult.java          # 统一分页
│       ├── dto/PageQuery.java           # 分页入参 {"pageNum","pageSize"}
│       ├── dto/IdRequest.java           # 按ID入参 {"id"}
│       └── exception/BizException.java  # 业务异常
├── positioning-gateway                  # 网关（WebFlux，端口 8080）
│   ├── GatewayApplication.java
│   ├── filter/RateLimitFilter.java      # Caffeine 令牌桶按IP限流
│   └── controller/FallbackController.java
├── positioning-auth
│   ├── positioning-auth-api             # Feign 契约 + DTO
│   │   ├── dto/UserDTO.java
│   │   └── client/UserFeignClient.java  # POST /internal/user/get
│   └── positioning-auth-biz             # 端口 8101，currentSchema=auth
│       ├── AuthApplication.java
│       ├── config/（Password/SaToken/StpInterface/MybatisPlus/MetaObjectHandler）
│       ├── entity/（6 张 auth 表实体）
│       ├── mapper/（6 个 Mapper，含角色/权限联查）
│       ├── service/（AuthService + impl）
│       ├── controller/（AuthController、internal/UserFeignController）
│       └── exception/GlobalExceptionHandler.java
└── positioning-business
    ├── positioning-business-api         # Feign 契约（预留）
    │   ├── dto/（MallDTO/SpaceDTO/ShopDTO）
    │   └── client/（Mall/Space/ShopFeignClient，POST /internal/.../get）
    └── positioning-business-biz         # 端口 8102，currentSchema=business
        ├── BusinessApplication.java     # @EnableFeignClients(UserFeignClient)
        ├── config/（SaToken/StpInterface/MybatisPlus/MetaObjectHandler）
        ├── entity/（18 张业务表实体）
        ├── mapper/（18 个 Mapper，含 PostGIS @Select/@Update）
        ├── service/（NavService 路径规划、ParkingRecordService 停车联动）
        ├── dto/（查询/请求/响应 DTO 共 18 个）
        ├── controller/
        │   ├── BaseCrudController.java  # 通用 POST CRUD 基类
        │   ├── 18 个业务控制器 + RouteController
        │   └── internal/（InternalMall/Space/ShopController，实现预留 Feign 契约）
        └── exception/GlobalExceptionHandler.java
```

### 3.3 服务注册与配置

| 服务 | 注册名 | 端口 | JDBC URL 后缀 | 默认账号 |
| --- | --- | --- | --- | --- |
| 网关 | positioning-gateway | 8080 | - | - |
| 认证服务 | positioning-auth | 8101 | `?currentSchema=auth` | postgres/123456 |
| 业务服务 | positioning-business | 8102 | `?currentSchema=business` | postgres/123456 |

环境变量：`DB_HOST/DB_PORT/DB_NAME/DB_USERNAME/DB_PASSWORD/NACOS_ADDR/NACOS_USERNAME/NACOS_PASSWORD/SA_TOKEN_SECRET`。

### 3.4 网关设计

- 路由：`/auth/**`、`/internal/**` → `lb://positioning-auth`；`/business/**` → `lb://positioning-business`。
- 限流：`RateLimitFilter`（GlobalFilter，order=-100），Caffeine 令牌桶按客户端 IP，默认 100 次/分钟（`positioning.gateway.rate-limit.capacity/refill-per-minute` 可调），超限返回 429 JSON。
- 熔断：每条路由挂 `CircuitBreaker` 过滤器 + `forward:/fallback/auth|business` 降级（Resilience4j 默认配置在 application.yml）。
- 跨域：globalcors 全放开（开发用）。
- 网关不校验 token（各服务用 Sa-Token 自校验），如需统一收敛可后续增强。

### 3.5 认证设计（Sa-Token + JWT）

- `POST /auth/register`：用户名/手机号唯一校验，BCrypt 加密，默认 `user_type=USER`、`status=1`。
- `POST /auth/login`：支持用户名或手机号；校验密码与状态；`StpUtil.login(userId)` 签发 JWT（`token-style: jwt-simple`，密钥 `SA_TOKEN_SECRET`）；写 `sys_login_log` 成功/失败日志；更新 `last_login_at`。
- `POST /auth/logout`、`POST /auth/me`：基于 `StpUtil`。
- 路由拦截：除 `/auth/login`、`/auth/register`、`/internal/**`、`/actuator/**`、`/error` 外全部要求登录。
- RBAC：`StpInterfaceImpl` 从 `sys_role/sys_user_role/sys_role_permission/sys_permission` 联查角色码与权限码。
- Feign 内部接口：`POST /internal/user/get`（JSON `{"id":...}`）返回 UserDTO，供业务服务调用。

### 3.6 业务设计

- 通用 CRUD：`BaseCrudController` 提供 `POST /page|get|create|update|delete`；专用控制器继承并叠加领域接口。
- 车位：`/query`（mallId/floorId/status 分页）、`/search`（space_no 模糊）、`/nearby`（PostGIS KNN 空闲车位）、`/get-geometry`、`/update-geometry`、`/occupy`、`/release`。
- 商铺：`/query`（多条件+关键词分页）、`/search`（pg_trgm ILIKE）、`/get-geometry`、`/update-geometry`。
- 信标：`/query`、`/by-floor`（ACTIVE 列表，供定位扫描）。
- 导航：节点 `POST /with-geometry`（`ST_GeomFromGeoJSON`）；路径规划 `POST /business/nav/route`（Dijkstra，跨层连接按双向折算成本参与寻路）。
- 停车：`/park`（入场，车位置 OCCUPIED）、`/end`（离场，车位置 FREE）、`/my`。
- 用户侧：收藏 `/my`、导航历史 `/record|/my`、定位会话 `/start|/end|/active`，均自动绑定当前登录用户（`StpUtil.getLoginIdAsLong()`）。
- 用户-商场绑定：`POST /business/mall-user/create` 先经 Feign 调认证服务校验用户；`/user-info` 跨服务聚合用户信息。
- 内部接口（预留契约实现）：`POST /internal/mall|space|shop/get`，已放行登录拦截。

### 3.7 统一接口约定

- **全部接口 POST，入参/出参均为 JSON**；响应结构 `{"code":200,"message":"操作成功","data":...}`。
- 分页入参 `{"pageNum":1,"pageSize":10}`；按 ID 操作 `{"id":1}`。
- 错误码：200 成功 / 400 参数或业务错误 / 401 未登录 / 403 无权限 / 404 不存在 / 429 限流 / 500 系统异常 / 503 服务不可用。

## 4. 关键实现细节

- **雪花 ID**：`@TableId(type = IdType.ASSIGN_ID)`；节点等手工构造用 `IdWorker.getId()`。
- **审计字段**：`MyMetaObjectHandler` 自动填充 `createdAt/updatedAt`（`strictInsertFill/strictUpdateFill`，无该字段的表自动跳过）。
- **逻辑删除**：`@TableLogic` + 全局配置 `logic-delete-field: deleted`。
- **分页**：`PaginationInnerInterceptor(DbType.POSTGRE_SQL)`；注意 MyBatis-Plus 3.5.9+ 需依赖 `mybatis-plus-jsqlparser`。
- **几何列**：实体中类型为 String 的几何字段统一 `@TableField(select=false, insertStrategy=NEVER, updateStrategy=NEVER)`，通用 CRUD 不触碰；空间读写走专用 SQL（`ST_AsGeoJSON` / `ST_GeomFromGeoJSON(...,0)` / `ST_Centroid` / `<->` KNN）。
- **Sa-Token JWT**：认证与业务服务 `application.yml` 中 `token-name: satoken`、`token-style: jwt-simple`、`jwt-secret-key` 相同；业务服务通过 `StpUtil.getLoginIdAsLong()` 取用户。
- **Feign**：business-biz 通过 `@EnableFeignClients(clients = UserFeignClient.class)` 调用 auth；auth 侧 `/internal/**` 已放行拦截。
- **网关包名坑**：Caffeine 的 Maven 坐标是 `com.github.ben-manes:caffeine`，但 Java 包名是 `com.github.benmanes.caffeine`（无连字符）。

## 5. 构建与运行

```bash
# 本机 JDK21 路径示例
$env:JAVA_HOME='C:\Users\54316\.jdks\jbr-21.0.11'
mvn clean package -DskipTests
```

启动顺序：先 Nacos、PostgreSQL（执行 sql 脚本）→ gateway → auth → business。

冒烟链路：

```bash
# 1 注册 → 2 登录拿 token → 3 业务分页 → 4 附近空闲车位 → 5 路径规划
curl -X POST http://127.0.0.1:8080/auth/register -H "Content-Type: application/json" \
  -d '{"username":"demo","password":"123456","nickname":"演示用户"}'
curl -X POST http://127.0.0.1:8080/auth/login -H "Content-Type: application/json" \
  -d '{"account":"demo","password":"123456"}'
curl -X POST http://127.0.0.1:8080/business/mall/page -H "Content-Type: application/json" -H "satoken: <token>" \
  -d '{"pageNum":1,"pageSize":10}'
curl -X POST http://127.0.0.1:8080/business/space/nearby -H "Content-Type: application/json" -H "satoken: <token>" \
  -d '{"mallId":1,"x":10.5,"y":20.3,"limit":5}'
curl -X POST http://127.0.0.1:8080/business/nav/route -H "Content-Type: application/json" -H "satoken: <token>" \
  -d '{"mallId":1,"fromNodeId":101,"toNodeId":205}'
```

## 6. 已知注意事项

- `sql/00_init.sql` 只装扩展不建库（用户已修改为连接后直接执行扩展安装），**不要改回**。
- 通用 CRUD 不写几何列：车位/商铺需先 `create` 再用几何接口更新；导航节点必须用 `with-geometry` 创建（`geom` NOT NULL）。
- 网关路由把 `/internal/**` 指向 auth，business 的 `/internal/**` 仅服务间 Feign 直接调用，不走网关。
- 业务服务 Sa-Token 拦截已放行 `/internal/**`（内部 Feign）。
- 本机 Maven 使用阿里云镜像（`~/.m2/settings.xml`），首次构建需联网下载依赖。
- 未实现项：单元测试、接口文档（OpenAPI）、种子数据、Docker Compose、定位算法、转向指令生成、网关统一鉴权。

## 7. 建议后续任务（供 DeepSeek Harness 执行）

1. 种子数据 SQL：商场/楼层/分区/车位/商铺/POI/信标/导航图示例（含 PostGIS 几何），并初始化管理员账号与 RBAC 权限。
2. 接口文档：在两个 biz 模块集成 Springdoc OpenAPI。
3. Docker Compose：Nacos + PostgreSQL(PostGIS) 一键环境。
4. 单元/集成测试：AuthService、NavService、ParkingRecordService、网关限流；PostGIS 查询用 Testcontainers。
5. 定位算法：beacon RSSI 指纹/三角定位与 positioning_session 联动。
6. 转向指令：基于节点坐标/`ST_Azimuth` 生成"直行/左转/右转/上楼"指令并扩展 RouteVO。
7. 可选：网关引入 Sa-Token 统一鉴权、车位状态 Redis 缓存、MyBatis-Plus 代码生成器。

## 8. 文件索引

| 文件 | 说明 |
| --- | --- |
| `README.md` | 项目总览与运行说明 |
| `pom.xml` | 父工程版本管理 |
| `docs/01-database-design.md` | 数据库设计文档 |
| `docs/DeepSeek-Harness-交接提示词.md` | 可直接粘贴给新执行环境的主提示词 |
| `docs/初始代码版本提示词.txt` | 用户历史提示词（勿动） |
| `docs/4.实体关系图.png` | 用户提供的 ER 图（勿动） |
| `sql/00_init.sql`、`sql/01_auth_db.sql`、`sql/02_business_db.sql` | 数据库脚本 |

<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.positioning.business.mapper.FloorConnectMapper">

    <!-- 查询某商场所有可用跨层连接（路径规划用） -->
    <select id="selectEnabledByMall" resultType="com.positioning.business.entity.FloorConnect">
        SELECT id, mall_id, from_floor_id, to_floor_id, connect_type, from_node_id, to_node_id, cost_m, status
        FROM floor_connect
        WHERE mall_id = #{mallId}
          AND status = 1
          AND deleted = 0
    </select>

</mapper>
